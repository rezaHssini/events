# Copy to keystore.properties (gitignored) or run:
#   bash deploy/ensure-android-keystore.sh
storeFile=event-release.keystore
storePassword=your-store-password
keyAlias=event-release
keyPassword=your-key-password
